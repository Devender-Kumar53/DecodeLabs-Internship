// use prisma-8

model User {
  id        Int               @id @default(autoincrement())
  email     String            @unique
  age       Int
  isActive  Boolean           @default(true)
  createdAt TimestamptzString @default(now())

  @@check(expression: "age >= 0", name: "user_age_non_negative")
}