// use prisma-8

model User {
  id           Int               @id @default(autoincrement())
  email        String            @unique
  passwordHash String
  createdAt    TimestamptzString @default(now())
}